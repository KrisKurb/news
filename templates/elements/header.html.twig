<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand logo_font" href="{{ path('index') }}">News</a>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNavDropdown">
            <ul class="navbar-nav justify-content-end col-6 col-xl-5">
                {% if app.user %}
                    <li class="nav-item default_font col-6 col-xl-4 p-0">
                        <a class="nav-link text-truncate" href="#">Hello, {{ app.user.name }}!</a>
                    </li>
                    <li class="nav-item default_font col-3">
                        <a class="nav-link" href="{{ path('app_logout') }}">Выйти</a>
                    </li>
                {% else %}
                    <li class="nav-item default_font col-3 text-center">
                        <a class="nav-link" href="{{ path('app_login') }}">Войти</a>
                    </li>
                    <li class="nav-item default_font col-3 text-center">
                        <a class="nav-link" href="{{ path('app_register') }}">Регистрация</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </nav>
</header>